#spring.application.name=apple-devices
server:
  port: 8080
  servlet:
    context-path: /api

spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/apple-store"
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: phuongnguyen20403@gmail.com
    password: ribt ynbg gzyr awoc
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
